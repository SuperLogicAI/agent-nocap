{
  "name": "agent-nocap",
  "version": "0.2.0",
  "description": "Your coding agent said \"all tests pass.\" Was that cap? Audits Claude Code and Codex transcripts for unbacked claims and hidden test failures.",
  "license": "MIT",
  "repository": {"type": "git", "url": "git+https://github.com/SuperLogicAI/agent-nocap.git"},
  "type": "module",
  "engines": {"node": ">=20"},
  "main": "dist/audit.js",
  "types": "dist/audit.d.ts",
  "exports": "./dist/audit.js",
  "bin": {"nocap": "dist/audit.js"},
  "publishConfig": {"access": "public"},
  "files": ["dist/audit.js", "dist/audit.d.ts"],
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/audit.test.js",
    "release": "npm run build && mkdir -p release && cp dist/audit.js release/nocap.mjs"
  },
  "devDependencies": {"@types/node": "26.6.1", "typescript": "5.9.3"}
}
